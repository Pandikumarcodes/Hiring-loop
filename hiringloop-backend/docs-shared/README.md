# Shared Documentation

These files are synchronized reference copies for Codex.

Authoritative documentation lives at the HiringLoop repository root and root `docs/` directories.

Do not independently edit architecture decisions in `docs-shared`.

Changes must first be made to root documentation and then synchronized.
